#!/usr/bin/env python

pos_total = 0
neg_total = 0
n = input()

while n != 0:
   if n > 0:
      pos_total = pos_total + n
   else:
      n < 0
      neg_total = neg_total + n
   n = input()

print neg_total, pos_total
